<?php
/**
 * Openpay — copiar a /var/www/sources/openpay/config.php
 * Permisos: chmod 640, no subir a git con valores reales.
 *
 * Sandbox: https://sandbox-api.openpay.mx/v1 — panel sandbox-dashboard.openpay.mx
 * Producción: https://api.openpay.mx/v1 — panel dashboard.openpay.mx
 */
return [
    // --- Sandbox (pruebas) — slots principales cuando production === false ---
    'merchant_id' => 'TU_MERCHANT_ID_SANDBOX',
    'private_key' => 'sk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX',
    'public_key' => 'pk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX',
    'production' => true,
    'base_url' => 'https://sandbox-api.openpay.mx/v1',

    // --- Producción (live) — obligatorio con production === true ---
    'production_merchant_id' => 'TU_MERCHANT_ID_LIVE',
    'production_private_key' => 'sk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX',
    'production_public_key' => 'pk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX',
    'production_base_url' => '',

    // Webhook (legacy / otros servicios; /precios usa precios-webhook.env)
    'webhook_secret' => 'whsec_XXXXXXXXXXXX',
    'webhook_username' => 'usuario_webhook',
    'webhook_password' => 'GENERAR_PASSWORD_SEGURO',
];

# Copiar a /var/www/sources/openpay/precios-webhook.env
# Mismas credenciales HTTP Basic al registrar el webhook en panel Openpay.
# chmod 640 && chown root:www-data

OPENPAY_WEBHOOK_USER=imcyc_precios_webhook
OPENPAY_WEBHOOK_PASSWORD=GENERAR_PASSWORD_WEBHOOK_SEGURO
